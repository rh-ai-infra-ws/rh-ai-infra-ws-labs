# 🚀 llm-d at Scale

llm-d is an intelligent orchestration layer above vLLM that adds prefix-cache aware routing, disaggregated prefill/decode, and advanced scheduling to maximize GPU utilization across a distributed inference fleet.
